defaults:
  - datamodule: argoverse2
  - model: vectornet
  - _self_
seed: 42

trainer:
  max_epochs: 5000
  accelerator: auto
  devices: auto
  default_root_dir: ./outputs
  log_every_n_steps: 50

optimizer:
  lr: 1e-4
