defaults:
  - _self_
  - datamodule: av2_smart
  - model: smart
  - trainer: default
  - logger: wandb
  - optimizer: adamw
  - experiment: null

project_name: smart
exp_name: test
seed: 42
mode: train
resume_from: null
output_root_dir: ./outputs/${project_name}/${exp_name}/${now:%Y-%m-%d_%H-%M-%S}
ckpt_dir: ${output_root_dir}/ckpt

hydra:
  run:
    dir: ${output_root_dir}
